import Foundation

// CFBundleExecutable target name. Kept in sync with Package.swift.
// This file exists so the bundling script and code agree on the app name.
enum AppInfo {
    static let name = "TheWatcher"
    static let bundleIdentifier = "com.winigee.thewatcher"
}
